version: 1
frontend:
  phases:
    build:
      commands:
        - cd portfolio\package.json 
        - npm install
        - npm run build
  artifacts:
    baseDirectory: /portfolio
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
